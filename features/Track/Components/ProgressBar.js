import * as React from 'react';
import { ProgressBar, MD3Colors } from 'react-native-paper';

const ProgressBarCmp = () => (
  <ProgressBar progress={0.5} color={"#054D82"} style={{height : 8 , borderRadius : 5 }}/>
);

export default ProgressBarCmp;